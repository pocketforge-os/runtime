# Shared runtime socket directory. Daemons must not declare RuntimeDirectory=pocketforge:
# systemd would re-own the directory whenever a service with a different User= starts.
d /run/pocketforge 0755 gamer gamer -
